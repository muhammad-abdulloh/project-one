import { publishFacade } from "@angular/compiler"

export interface Product {
    productName: string
    category: string,
    condition: string,
    price: number,
    comment: string,
    date: string,
    id: number
}

